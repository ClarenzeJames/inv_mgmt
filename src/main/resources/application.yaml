spring:
  application:
    name: inventory_management

  datasource:
    url: jdbc:postgresql://localhost:5432/inv_mgmt
#    driver-class-name: org.postgresql.Driver
    username: admin
    password: password
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none
    show-sql: true
  sql:
    init:
      mode: always